I couldn't set some preferences that I wanted to set, such as setting up borders and such for label and textviews, thus I used background colors just to showcase the different objects and their borders. Now, despite being horribly unfashionable, I still recognize that these colors are ugly and they are only temporary and for this version of the application.

Also, I hope it's not an issue, but i added the data sources to the repo and the icons that i plan on using and a file named 'Alghusson-Saleh-Prob1-AppIdea.txt' that has the answers to the write up requested in the assignment.

Notes:
	Custom icon guide:
		https://developer.apple.com/ios/human-interface-guidelines/graphics/custom-icons/
	Look at photos app for layout inspiration

As far as I know, I have implemented everything required except:
		* nil

Features not working properly:
		* nil

Features to add:

		* support multiple flags for language selection based on region
		* Search button
		* Speech synth
		* Add sommerville/other cities data
		* Localization: Add right to left support for arabic/hebrew

		About View:
		Detail View:
			* Gap in top of map
			* Change the 3 labels to textview
			* More info button

		Map View:
      * Highlight Cambridge
			* highlight zip codes
			* User location button
		Settings View:
			* Night mode?
			* Custom pin colors app if it has a base language that's not supported.

		Table View:
			* variable cell size

Known Bugs:
		* Annotation only shows 1 art piece. Even if the location has multiple
